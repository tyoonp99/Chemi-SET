# 🎨 CHEMI-SET UI GUIDELINES

> 문서 버전: 1.0  
> 기준 구현: `title.tscn`, `game_ui.tscn`, HUD·공통 팝업 씬 및 현재 `.tres` 리소스

이 문서는 현재 적용된 Chemi-SET UI의 시각·구성 규칙을 기록한다. 구조와 코드 책임은 `PROJECT_RULES.md`를 따른다.

---

## 1. 디자인 방향

- 세로형 모바일 화면을 기준으로 한다. 기준 뷰포트는 `720 × 1280`이다.
- 어두운 남색 기반 위에 밝은 점수·시간 정보와 스카이블루 강조 버튼을 사용한다.
- 아이콘은 불필요한 이미지 대신 텍스트 이모지 또는 단색 SVG를 우선한다.
- 중요한 행동은 메인 버튼, 되돌리거나 보조적인 행동은 서브 버튼을 사용한다.

---

## 2. 색상 토큰

| 용도 | 색상 | 현재 적용 위치 |
| :--- | :--- | :--- |
| 게임 배경 | `#1a2436` | `mode_speed.tscn` 배경 |
| HUD 배경 | `#151d2c` | `game_hud.tscn` 상단 바 |
| 팝업 패널 | `#232f3e` | `popup_panel_style.tres` |
| 메인 버튼 | `#48bdf5` | `main_button_style.tres` |
| 메인 버튼 글자 | `#111111` | 메인 버튼 씬 연결부 |
| 서브 버튼 | `#303e4d` | `sub_button_style.tres` |
| 서브 버튼 글자 | `#ffffff` | 서브 버튼 씬 연결부 |
| 보조 라벨 | `#8a9bb3` | HUD의 점수·시간 제목 |
| 게임 팝업 덮개 | `#000000`, Alpha 150 | 공통 팝업 `ColorRect` |

### 타이틀 화면의 모드 색상

타이틀의 세 모드 버튼은 모드 구분을 위해 아래 색상을 유지한다.

| 버튼 | 색상 |
| :--- | :--- |
| 1분 모드 | `#52bdec` |
| 3분 모드 | `#75bb68` |
| 연습 모드 | `#f9b245` |

새 모드 버튼을 추가할 때는 기존 모드 버튼과 명확히 구분되면서도, 텍스트 대비가 확보되는 색상을 사용한다.

---

## 3. 타이포그래피

- 기본 폰트: `Pretendard-Medium.ttf`, 기본 크기 28
- 게임 로고: `Pretendard-ExtraBold.ttf`, 80
- HUD 값: `Pretendard-Bold.ttf`, 48
- HUD 제목: 28
- 팝업 제목: 32~40
- 팝업 주요 행동 버튼: 28

본문·버튼·라벨은 가능한 28 이상을 사용한다. HUD 보조 정보나 좁은 영역의 보조 버튼처럼 28을 유지하면 레이아웃·가독성이 나빠지는 경우에만 더 작은 크기를 허용한다.

텍스트는 짧고 행동 중심으로 작성한다. 예: `▶ 계속하기`, `↺ 재시작`, `🏠 타이틀로`.

---

## 4. 공통 컴포넌트

### HUD

- 화면 상단 전체 폭을 사용하며 높이는 137이다.
- 좌우 여백 60, 상단 여백 20, 하단 여백 12를 사용한다.
- 점수와 시간은 동일한 비중으로 배치한다.
- 설정 버튼은 우측 상단에 한 개만 둔다.
  - 최소 크기: `80 × 80`
  - 서브 버튼 스타일 사용
  - `modulate` Alpha: 약 0.59
  - 설정 SVG 아이콘을 중앙 정렬

### 팝업

- 전체 화면 `ColorRect`로 배경을 덮고, 검정 Alpha 150을 사용한다.
- `CenterContainer` 안에 `PanelContainer`를 배치해 항상 중앙에 정렬한다.
- 패널은 `popup_panel_style.tres`를 사용한다.
  - 배경 `#232f3e`
  - 모서리 반경 24
  - 내부 여백: 좌우 30, 상하 40
- 팝업 내부 요소 간 기본 간격은 20이다.
- 게임을 멈춘 상태에서 조작해야 하는 `PausePopup`은 `Process Mode = Always`를 사용한다.

### 버튼

- 메인 버튼: `main_button_style.tres`, 반경 10, 글자 `#111111`
- 서브 버튼: `sub_button_style.tres`, 반경 10, 글자 흰색
- 버튼 내부 여백: 좌우 30, 상하 15
- 결과 팝업의 버튼은 한 줄에 배치하고, 버튼 사이 간격은 12를 사용한다.

### 최고 점수 입력

- 팝업 너비 기준 최소값은 380이다.
- 이니셜 입력은 3글자로 제한하고 중앙 정렬한다.
- 제출은 메인 버튼 `▶ 등록`을 사용한다.

---

## 5. 타이틀 화면

- 로고와 모드 버튼은 화면 중앙 `VBoxContainer`에 배치한다.
- 모드 버튼 최소 크기는 `400 × 80`이다.
- 설정 버튼은 우측 하단에 고정한다.
  - Offset: Left `-104`, Top `-104`, Right `-40`, Bottom `-40`
  - `modulate` Alpha: 약 0.59
- 튜토리얼·설정·랭킹 팝업은 전체 화면 오버레이 방식으로 표시한다.

타이틀의 기존 팝업 스타일은 공통 게임 팝업과 일부 색상·구조가 다르다. 타이틀 UI를 공통 디자인 시스템으로 통합하는 작업은 별도 변경으로 진행한다.

---

## 6. 리소스 사용 규칙

- 공통 테마: `game_ui_theme.tres`
- 메인 버튼: `main_button_style.tres`
- 서브 버튼: `sub_button_style.tres`
- 팝업 패널: `popup_panel_style.tres`
- 스타일 수치 변경은 해당 `.tres`에서 수행하고, 동일 목적의 새 인라인 `StyleBoxFlat`은 만들지 않는다.
- 새 UI 씬은 공통 `.tres`를 외부 리소스로 연결한다.

---

## 7. 상호작용 원칙

- 모드 씬은 UI를 직접 갱신하지 않고 신호를 통해 중앙 매니저에 상태를 전달한다.
- HUD와 팝업의 표시·숨김은 `UIManager` 공개 API를 사용한다.
- UI 스크립트의 노드 참조는 `%UniqueName`만 사용한다.
- 새 버튼에는 명확한 행동 텍스트 또는 아이콘을 사용하고, 기능이 없는 버튼은 배치하지 않는다.

향후 버튼 터치 텐션, 정답 반짝임, 오답 흔들림을 추가할 때도 기존 레이아웃과 색상 토큰을 유지한다.
